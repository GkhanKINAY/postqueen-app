# Revisions that only reformat. `git blame` skips them so it keeps pointing at
# whoever last changed the code, not at whoever last reindented it.
#
# Turn it on once per clone:
#
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Add a revision here only if it changes no behaviour at all.

# build(frontend): move to Tailwind 4
#
# Dropping Sass meant rewriting 332 `//` comments as `/* */` across
# global.css and colors.css, which touches almost every line of both.
0faf61eb114f6b0572ff9129a21663464a6b0549
