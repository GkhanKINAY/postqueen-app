import React from 'react';

// Testimonials intentionally emptied during the PostQueen rebrand — the
// upstream quotes praised the upstream product and cannot be attributed to
// this one. Refill with real PostQueen testimonials when available.
type Testimonial = {
  picture: string;
  name: string;
  description: string;
  content: React.ReactNode;
};

export const testimonials1: Testimonial[] = [];
export const testimonials2: Testimonial[] = [];
